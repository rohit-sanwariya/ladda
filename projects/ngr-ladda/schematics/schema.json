{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "id": "ng-add",
    "title": "Add NGR Ladda",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project."
        }
    },
    "required": ["project"]
}